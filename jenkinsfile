pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Package') {
                    steps {
                        echo "mvn clean package"
                    }
        }
    }
}