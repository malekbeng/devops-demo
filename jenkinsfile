pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test Qualite ') {
                            steps {
                                echo "mvn clean verify sonar:sonar -Dsonar.projectKey=dev -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_e72aac4f0643c8df7512ab0fbc6cdf41da7b1983"
                            }
        }
        stage('Package') {
                            steps {
                                echo "mvn clean package"
                            }
        }
    }
}