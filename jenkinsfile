pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test Qualite ') {
                            steps {
                                echo "mvn clean verify sonar:sonar -Dsonar.projectKey=Test  -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_08b504d22632922158ebe47818d09799beef0d07"
                            }
        }
        stage('Package') {
                            steps {
                                echo "mvn clean package"
                            }
        }
    }
}