pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test Qualite ') {
                            steps {
                                echo "mvn clean verify sonar:sonar -Dsonar.projectKey=devopsTest -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_f96737edb2d0773be30ffc57a49d65b0d17227e0"
                            }
        }
        stage('Package') {
                            steps {
                                echo "mvn clean package"
                            }
        }
    }
}