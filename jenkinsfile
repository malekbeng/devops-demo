pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test Qualite ') {
                            steps {
                                echo "mvn clean verify sonar:sonar -Dsonar.projectKey=devopsTest2  -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_01761e5d10919a4b1de9cef17834dc78e71f41c2"
                            }
        }
        stage('Package') {
                            steps {
                                echo "mvn clean package"
                            }
        }
    }
}