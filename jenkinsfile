pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test Qualite ') {
                            steps {
                                echo "mvn clean verify sonar:sonar -Dsonar.projectKey=dev -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_0020c8a130c64c73cad948d068bae9d0876e95a6"
                            }
        }
        stage('Package') {
                            steps {
                                echo "mvn clean package"
                            }
        }
    }
}