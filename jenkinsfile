pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test') {
                    steps {
                        echo "mvn clean package"
                    }
        }
    }
}