pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo "mvn clean test"
            }
        }
        stage('Test Qualite ') {
                            steps {
                                echo "mvn clean verify sonar:sonar -Dsonar.projectKey=devopsTest -Dsonar.host.url=http://localhost:9000 -Dsonar.login=sqp_ad5ab964545e81fb8dc19f7477f263cdff652f3e"
                            }
        }
        stage('Package') {
                            steps {
                                echo "mvn clean package"
                            }
        }
    }
}